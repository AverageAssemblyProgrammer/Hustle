#!/usr/bin/hustle/hustle.py runme
include("all") return 

func check_subcommand(subcommand)
    var c = subcommand
    if str(c) == "hello" then 
        printh("hello")
    end 
end 

var subcommand = argv(1)
check_subcommand(subcommand)
